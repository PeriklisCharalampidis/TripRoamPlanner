{% extends 'base.html.twig' %}

{% block title %}JournalPost{% endblock %}

{% block body %}
    <h1>JournalPost</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ journal_post.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ journal_post.date ? journal_post.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ journal_post.text }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ journal_post.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_journal_post_index') }}">back to list</a>

    <a href="{{ path('app_journal_post_edit', {'id': journal_post.id}) }}">edit</a>

    {{ include('journal_post/_delete_form.html.twig') }}
{% endblock %}

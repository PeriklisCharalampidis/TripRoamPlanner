<form method="post" action="{{ path('app_paking_list_delete', {'id': packing_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ packing_item.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>

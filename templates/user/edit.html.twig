{% extends 'base.html.twig' %}

{% block title %}Edit User
{% endblock %}

{% block body %}
	<h1>Setting</h1>
	<div class="container">
		<div
			class="flex-row">
			{# {{ include('user/_form.html.twig', {'button_label': 'Done'}) }} #}
			{{ form_start(form) }}
			{{ form_widget(form) }}

			<a class="btn btn-outline-warning mt-3" href "{{ path('app_user_show') }}">Back
			</a>

			<button class="btn btn-outline-warning mt-3">{{ button_label|default('Done') }}</button>
			{{ include('user/_delete_form.html.twig') }}
			{{ form_end(form) }}


		</div>

	</div>
{% endblock %}
